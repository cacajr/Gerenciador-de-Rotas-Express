//cod. feito para evitar que o server seja iniciado várias vezes
//falta exportar no server.js a execucao da funcao express com o end. desse arq. (express(./end desse arq))
/*
const express = require("express");

const app = express();

const routes = require("../app/routes");

app.use("/", routes);

module.exports = app;
*/